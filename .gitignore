node_modules/
.next/
plugins/mfe-gateway/src/_plugins/
plugins/mfe-gateway/dist/
deploy/scratch/
*.tsbuildinfo

# Ignore environment variables
.env
.env.*
!.env.example
!.env.local.example

# Traefik rendered static config (chứa email + domain thật, sinh bởi setup.sh
# từ traefik.dev.yml / traefik.prod.yml). Chỉ commit template, không commit file render.
deploy/traefik/traefik.yml


# Ignore backups
deploy/backups/

# Auto-generated session transcripts (may contain secrets)
session-*.md
