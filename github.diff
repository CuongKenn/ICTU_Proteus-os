diff --git a/.github/workflows/backend-ci.yml b/.github/workflows/backend-ci.yml
index 992a55a..7f2a517 100644
--- a/.github/workflows/backend-ci.yml
+++ b/.github/workflows/backend-ci.yml
@@ -14,18 +14,34 @@ on:
       - '.github/workflows/backend-ci.yml'
   pull_request:
     branches: [main, develop]
-    paths:
-      - 'core-engine/backend/**'
-      - '.github/workflows/backend-ci.yml'
-
-defaults:
-  run:
-    working-directory: core-engine/backend
+    # Không có paths filter trên PR — để backend-ci summary job luôn chạy
+    # (Required check không được dùng paths filter — sẽ stuck "waiting" nếu workflow không trigger)
 
 jobs:
+  # Phát hiện xem có thay đổi backend không — chạy luôn trên mọi PR
+  detect-changes:
+    name: Detect Backend Changes
+    runs-on: ubuntu-latest
+    outputs:
+      backend: ${{ steps.filter.outputs.backend }}
+    steps:
+      - uses: actions/checkout@v4
+      - uses: dorny/paths-filter@v3
+        id: filter
+        with:
+          filters: |
+            backend:
+              - 'core-engine/backend/**'
+              - '.github/workflows/backend-ci.yml'
+
   lint:
     name: Lint & Format Check
     runs-on: ubuntu-latest
+    needs: detect-changes
+    if: needs.detect-changes.outputs.backend == 'true'
+    defaults:
+      run:
+        working-directory: core-engine/backend
 
     steps:
       - name: Checkout code
@@ -94,6 +110,9 @@ jobs:
     name: Alembic Migration Verification
     runs-on: ubuntu-latest
     needs: lint
+    defaults:
+      run:
+        working-directory: core-engine/backend
 
     services:
       postgres:
@@ -151,28 +170,43 @@ jobs:
 
       - name: Run Alembic migrations (upgrade head)
         run: |
-          echo "=== Running: alembic upgrade head ==="
-          alembic upgrade head
-          echo "=== Migration applied successfully ==="
+          if [ -f "alembic.ini" ]; then
+            echo "=== Running: alembic upgrade head ==="
+            alembic upgrade head
+            echo "=== Migration applied successfully ==="
+          else
+            echo "::warning::No alembic.ini found. Skipping migrations."
+          fi
 
       - name: Verify migration state
         run: |
-          echo "=== Current Alembic head ==="
-          alembic current
-          echo ""
-          echo "=== Migration history ==="
-          alembic history --verbose
+          if [ -f "alembic.ini" ]; then
+            echo "=== Current Alembic head ==="
+            alembic current
+            echo ""
+            echo "=== Migration history ==="
+            alembic history --verbose
+          else
+            echo "Skipping verification (no alembic.ini)"
+          fi
 
       - name: Check for model drift
         run: |
-          alembic check 2>&1 || {
-            echo "::warning::Model drift detected! Run: alembic revision --autogenerate -m 'description'"
-          }
+          if [ -f "alembic.ini" ]; then
+            alembic check 2>&1 || {
+              echo "::warning::Model drift detected! Run: alembic revision --autogenerate -m 'description'"
+            }
+          else
+            echo "Skipping drift check (no alembic.ini)"
+          fi
 
   test:
     name: Tests & Coverage
     runs-on: ubuntu-latest
     needs: lint
+    defaults:
+      run:
+        working-directory: core-engine/backend
 
     services:
       postgres:
@@ -262,9 +296,23 @@ jobs:
           retention-days: 7
 
   backend-ci:
-    name: backend-ci ✅
+    name: backend-ci
     runs-on: ubuntu-latest
-    needs: [lint, security, migration, test]
+    # Luôn chạy — kể cả khi không có thay đổi backend (tránh PR stuck "waiting")
+    if: always()
+    needs: [detect-changes, lint, security, migration, test]
     steps:
-      - name: All backend checks passed
-        run: echo "All backend checks passed successfully"
+      - name: Evaluate backend CI result
+        run: |
+          if [[ "${{ needs.detect-changes.outputs.backend }}" == "false" ]]; then
+            echo "⏩ No backend changes — skipping backend-ci checks (pass)"
+            exit 0
+          fi
+          if [[ "${{ needs.lint.result }}" == "failure" ||
+                "${{ needs.security.result }}" == "failure" ||
+                "${{ needs.migration.result }}" == "failure" ||
+                "${{ needs.test.result }}" == "failure" ]]; then
+            echo "❌ One or more backend checks failed"
+            exit 1
+          fi
+          echo "✅ All backend checks passed"
diff --git a/.github/workflows/dependency-review.yml b/.github/workflows/dependency-review.yml
deleted file mode 100644
index 32cf3af..0000000
--- a/.github/workflows/dependency-review.yml
+++ /dev/null
@@ -1,44 +0,0 @@
-# Copyright (c) 2026 CuongKenn & ICTU Team
-# SPDX-License-Identifier: AGPL-3.0-or-later
-#
-# Dependency Review — Scan CVE vulnerabilities trong dependencies mới trên PR
-# Fail nếu có dependency có CVE severity >= HIGH
-
-name: Dependency Review
-
-on:
-  pull_request:
-    branches: [main, develop]
-    paths:
-      - 'core-engine/backend/requirements.txt'
-      - 'core-engine/frontend/package.json'
-      - 'core-engine/frontend/package-lock.json'
-      - 'plugins/**/requirements.txt'
-
-permissions:
-  contents: read
-  pull-requests: write
-
-jobs:
-  dependency-review:
-    name: Dependency Security Review
-    runs-on: ubuntu-latest
-
-    steps:
-      - name: Checkout repository
-        uses: actions/checkout@v4
-
-      - name: Dependency Review (GitHub Native)
-        uses: actions/dependency-review-action@v4
-        with:
-          # Fail nếu có CVE HIGH hoặc CRITICAL
-          fail-on-severity: high
-          # Cho phép các licenses phổ biến
-          allow-licenses: >-
-            MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC,
-            Python-2.0, PSF-2.0, LGPL-2.1-or-later, MPL-2.0,
-            AGPL-3.0-or-later, GPL-3.0-or-later
-          # Comment lên PR với summary
-          comment-summary-in-pr: always
-          # Warn nhưng không fail nếu chỉ có LOW/MODERATE
-          warn-only: false
diff --git a/.github/workflows/frontend-ci.yml b/.github/workflows/frontend-ci.yml
index 69c449d..0724c75 100644
--- a/.github/workflows/frontend-ci.yml
+++ b/.github/workflows/frontend-ci.yml
@@ -14,27 +14,43 @@ on:
       - '.github/workflows/frontend-ci.yml'
   pull_request:
     branches: [main, develop]
-    paths:
-      - 'core-engine/frontend/**'
-      - '.github/workflows/frontend-ci.yml'
-
-defaults:
-  run:
-    working-directory: core-engine/frontend
+    # Không có paths filter trên PR — để frontend-ci summary job luôn chạy
+    # (Required check không được dùng paths filter — sẽ stuck "waiting" nếu workflow không trigger)
 
 jobs:
+  # Phát hiện xem có thay đổi frontend không — chạy luôn trên mọi PR
+  detect-changes:
+    name: Detect Frontend Changes
+    runs-on: ubuntu-latest
+    outputs:
+      frontend: ${{ steps.filter.outputs.frontend }}
+    steps:
+      - uses: actions/checkout@v4
+      - uses: dorny/paths-filter@v3
+        id: filter
+        with:
+          filters: |
+            frontend:
+              - 'core-engine/frontend/**'
+              - '.github/workflows/frontend-ci.yml'
+
   lint:
     name: Lint & Type Check
     runs-on: ubuntu-latest
+    needs: detect-changes
+    if: needs.detect-changes.outputs.frontend == 'true'
+    defaults:
+      run:
+        working-directory: core-engine/frontend
 
     steps:
       - name: Checkout code
         uses: actions/checkout@v4
 
-      - name: Setup Node.js 20
+      - name: Setup Node.js 22
         uses: actions/setup-node@v4
         with:
-          node-version: 20
+          node-version: 22
           cache: 'npm'
           cache-dependency-path: core-engine/frontend/package-lock.json
 
@@ -55,15 +71,19 @@ jobs:
     name: Vitest Unit Tests
     runs-on: ubuntu-latest
     needs: lint
+    if: needs.detect-changes.outputs.frontend == 'true'
+    defaults:
+      run:
+        working-directory: core-engine/frontend
 
     steps:
       - name: Checkout code
         uses: actions/checkout@v4
 
-      - name: Setup Node.js 20
+      - name: Setup Node.js 22
         uses: actions/setup-node@v4
         with:
-          node-version: 20
+          node-version: 22
           cache: 'npm'
           cache-dependency-path: core-engine/frontend/package-lock.json
 
@@ -81,16 +101,20 @@ jobs:
   build:
     name: Next.js Production Build
     runs-on: ubuntu-latest
-    needs: lint
+    needs: [detect-changes, lint, test]
+    if: needs.detect-changes.outputs.frontend == 'true'
+    defaults:
+      run:
+        working-directory: core-engine/frontend
 
     steps:
       - name: Checkout code
         uses: actions/checkout@v4
 
-      - name: Setup Node.js 20
+      - name: Setup Node.js 22
         uses: actions/setup-node@v4
         with:
-          node-version: 20
+          node-version: 22
           cache: 'npm'
           cache-dependency-path: core-engine/frontend/package-lock.json
 
@@ -125,9 +149,22 @@ jobs:
           retention-days: 3
 
   frontend-ci:
-    name: frontend-ci ✅
+    name: frontend-ci
     runs-on: ubuntu-latest
-    needs: [lint, test, build]
+    # Luôn chạy — kể cả khi không có thay đổi frontend (tránh PR stuck "waiting")
+    if: always()
+    needs: [detect-changes, lint, test, build]
     steps:
-      - name: All frontend checks passed
-        run: echo "All frontend checks passed successfully"
+      - name: Evaluate frontend CI result
+        run: |
+          if [[ "${{ needs.detect-changes.outputs.frontend }}" == "false" ]]; then
+            echo "⏩ No frontend changes — skipping frontend-ci checks (pass)"
+            exit 0
+          fi
+          if [[ "${{ needs.lint.result }}" == "failure" ||
+                "${{ needs.test.result }}" == "failure" ||
+                "${{ needs.build.result }}" == "failure" ]]; then
+            echo "❌ One or more frontend checks failed"
+            exit 1
+          fi
+          echo "✅ All frontend checks passed"
diff --git a/.github/workflows/pr-check.yml b/.github/workflows/pr-check.yml
index bb16929..2366fb7 100644
--- a/.github/workflows/pr-check.yml
+++ b/.github/workflows/pr-check.yml
@@ -12,7 +12,7 @@ on:
 
 jobs:
   validate-pr:
-    name: Validate PR Title, Body & Issue Link
+    name: validate-pr
     runs-on: ubuntu-latest
     permissions:
       pull-requests: write  # Để comment lên PR nếu lỗi
