# Keycloak 26 đã xóa theme keycloak.v3/common/keycloak.v3 (chỉ còn base + keycloak PF5).
# parent cũ khiến theme proteus load fail và rơi về built-in. CSS custom dùng class
# pf-v5-c-* nên parent=keycloak là đúng.
parent=keycloak
styles=css/styles.css
