{
  "_license": "Copyright (c) 2026 CuongKenn & ICTU Team | SPDX-License-Identifier: AGPL-3.0-or-later",
  "name": "Asset Overview",
  "description": "Metabase dashboard for Asset Overview",
  "cards": [
    {
      "name": "Tổng Tài Sản",
      "display": "scalar",
      "dataset_query": {
        "type": "native",
        "native": {
          "query": "SELECT COUNT(*) FROM ast_assets WHERE deleted_at IS NULL",
          "template-tags": {"tenant_id": {"type": "text", "required": true}}
        }
      }
    },
    {
      "name": "Tài Sản Theo Trạng Thái",
      "display": "pie",
      "dataset_query": {
        "type": "native",
        "native": {
          "query": "SELECT status, COUNT(*) as count FROM ast_assets WHERE deleted_at IS NULL GROUP BY status",
          "template-tags": {"tenant_id": {"type": "text", "required": true}}
        }
      }
    },
    {
      "name": "Tài Sản Theo Phòng Ban",
      "display": "bar",
      "dataset_query": {
        "type": "native",
        "native": {
          "query": "SELECT location, COUNT(*) as count FROM ast_assets WHERE deleted_at IS NULL GROUP BY location ORDER BY count DESC",
          "template-tags": {"tenant_id": {"type": "text", "required": true}}
        }
      }
    }
  ],
  "collection": "asset-module",
  "parameters": [{"name": "tenant_id", "slug": "tenant_id", "type": "string", "required": true}]
}
