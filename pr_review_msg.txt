❌ **PR DEEP REVIEW FAILED: Kiến trúc vi phạm & Thiếu tuân thủ Open Source**

Sau quá trình rà soát 10 vòng, PR này bị từ chối (Request Changes) do vi phạm các nguyên tắc cốt lõi đã định nghĩa trong `AGENTS.md`. Vui lòng khắc phục các lỗi sau:

### 1. Vi phạm Hexagonal Architecture (Nghiêm trọng)
- **File**: `core-engine/backend/app/entrypoints/routers/plugins.py` | **Line**: 98
- **Chi tiết**: Tại Router `configure_plugin_credentials`, bạn đang khởi tạo và gọi trực tiếp `N8nAdapter`. Điều này vi phạm nghiêm trọng quy tắc: *"Cấm gọi trực tiếp Database hay thư viện bên ngoài từ Controller/Router. Mọi giao tiếp phải thông qua Use Cases và Adapters."*
- **Đề xuất**: Tạo một Use Case mới (ví dụ: `ConfigurePluginCredentialsUseCase`) và gọi Use Case này từ Router. Inject `N8nAdapter` vào Use Case thông qua interface. Ngoài ra, biến `plugin_id` trên route đang không được sử dụng ở bất kỳ đâu trong hàm.

### 2. Thiếu Header Bản quyền (Nghiêm trọng)
- **File**: `core-engine/backend/tests/adapters/external/test_n8n_adapter.py`
- **File**: `core-engine/backend/tests/entrypoints/routers/test_plugins_api.py`
- **Chi tiết**: Các file mã nguồn mới tạo BẮT BUỘC phải có thông tin Copyright & License ở các dòng đầu tiên để tuân thủ pháp lý Open-Source.
- **Đề xuất**: Thêm 2 dòng sau vào đầu mỗi file:
  ```python
  # Copyright (c) 2026 CuongKenn & ICTU Team
  # SPDX-License-Identifier: AGPL-3.0-or-later
  ```

### 3. Thiếu cập nhật CHANGELOG & API Docs (Bắt buộc)
- **File**: `CHANGELOG.md` & `docs/api-swagger.yaml`
- **Chi tiết**: Bạn đã thêm một endpoint mới `POST /api/v1/plugins/{plugin_id}/credentials` nhưng hoàn toàn không cập nhật `CHANGELOG.md` và Swagger documentation.
- **Đề xuất**: 
  1. Thêm mô tả endpoint mới vào `docs/api-swagger.yaml`.
  2. Bổ sung mục `### Added` trong `CHANGELOG.md` ghi nhận việc thêm endpoint và tính năng cấu hình Credentials trên UI.
